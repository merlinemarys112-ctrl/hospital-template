/**
 * @deprecated Prefer `lib/mappers/clinic-mapper.ts` (JSON:API).
 * Kept as a thin re-export so older import scripts keep resolving.
 */
export { mapDrupalClinicResource as mapDrupalClinic } from '../lib/mappers/clinic-mapper'
export type { MappedClinic } from '../lib/mappers/clinic-mapper'
